package org.firstinspires.ftc.teamcode.autonomous;


import org.EverglowLibrary.utils.PointD;
import org.EverglowLibrary.utils.Pose;

public class sketchBasicAutonomous {

    boolean startLeft = false;

    PointD unloadLocation = new PointD(//, //);
    Pose startPosition = new Pose (//, //, PAI / 2);
    if(startLeft) {
        startPosition.angle = -PAI / 2;
        unloadLocation += //;
        startPosition.x += //;
    }

    if(startBack) {
        startPosition.y -= //;
    }

}
